type SearchPageProps = {
  searchParams: {
    title: string;
    categoryId: string;
  };
};

const SearchPage = ({ searchParams }: SearchPageProps) => {
  return <>SearchPage</>;
};

export default SearchPage;
